# Perfume CRUD (Lab 5)

cd .\server\
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python main.py

API:
GET /orders
GET /orders/<id>
POST /orders
PATCH/PUT /orders/<id>
DELETE /orders/<id>
